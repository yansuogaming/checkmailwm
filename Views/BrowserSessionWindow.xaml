<ui:FluentWindow x:Class="CheckMailWM2.Views.BrowserSessionWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                 Title="Khởi Tạo Phiên Trình Duyệt &amp; Giải Captcha - Walmart"
                 Height="850" Width="1100"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0"
                     Title="Walmart Session &amp; Captcha Warm-up"
                     Icon="/app_icon.ico" />

        <!-- Control Bar -->
        <Border Grid.Row="1" Background="#1E293B" Padding="14,10" BorderBrush="#334155" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                    <ui:SymbolIcon Symbol="ShieldCheckmark24" Foreground="#38BDF8" Margin="0,0,8,0" FontSize="20" />
                    <TextBlock x:Name="txtStatus"
                               Text="💡 Hãy giải Captcha (Press &amp; Hold) nếu có. Khi đến form đăng nhập, cửa sổ sẽ TỰ ĐỘNG LƯU VÀ ĐÓNG!"
                               Foreground="#38BDF8"
                               FontWeight="SemiBold"
                               FontSize="13"
                               VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                    <ui:Button x:Name="btnAutoSolve"
                               Content="🤖 Tự giải Captcha (15s)"
                               Icon="{ui:SymbolIcon PlayCircle24}"
                               Appearance="Primary"
                               FontWeight="SemiBold"
                               Margin="0,0,8,0"
                               Click="BtnAutoSolve_Click" />

                    <ui:Button x:Name="btnRefresh"
                               Content="Làm mới"
                               Icon="{ui:SymbolIcon ArrowClockwise20}"
                               Appearance="Secondary"
                               Margin="0,0,8,0"
                               Click="BtnRefresh_Click" />

                    <ui:Button x:Name="btnLogin"
                               Content="Vào Login"
                               Icon="{ui:SymbolIcon Person20}"
                               Appearance="Secondary"
                               Margin="0,0,8,0"
                               Click="BtnLogin_Click" />

                    <ui:Button x:Name="btnDone"
                               Content="LƯU PHIÊN &amp; ĐÓNG"
                               Icon="{ui:SymbolIcon Checkmark20}"
                               Appearance="Success"
                               FontWeight="Bold"
                               Click="BtnDone_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Progress -->
        <ProgressBar Grid.Row="2"
                     x:Name="progressBar"
                     IsIndeterminate="True"
                     Height="3"
                     Foreground="#38BDF8"
                     Visibility="Visible" />

        <!-- WebView2 Browser Control -->
        <wv2:WebView2 Grid.Row="3" x:Name="webView" />
    </Grid>
</ui:FluentWindow>
